# AI Knowledge Base
*Last updated: 2026-06-18*

## Reasoning & Planning
- (No durable updates yet — populate as briefs cover planning/search/MCTS/hierarchical-planning developments.)

## Memory Systems
- ChatGPT "Dreaming V3" (rolled out 2026-06-04, pre-window for this brief series but noted for context): hierarchical memory architecture that synthesizes context automatically across a user's full conversation history during idle/"dreaming" periods, vs. prior explicit-remember model. Reported gains: 5x compute efficiency, 82.8% factual recall, 71.3% preference adherence, 75.1% time-sensitive accuracy. Watch for competitive responses (Claude, Gemini) adopting similar background-consolidation memory architectures.

## Retrieval & RAG
- (No durable updates yet.)

## Tool Use & Protocols
- **MCP schema fidelity**: OpenAI Codex (2026-06-10) fixed handling of `oneOf`/`allOf` in MCP tool/connector input schemas — previously, schema compaction for context-budget reasons could flatten polymorphic schemas, causing tool calls with wrong argument shapes against complex connectors. Implication: this was a real, shipping bug class across at least one major agent client; worth checking other clients (Claude Code, etc.) for the same issue. Codex also added standalone web search callable from "code mode," including from nested JS tool calls.
- **MCP tool-output provenance gap — "Agentjacking" (NEW, disclosed 2026-06-12, Sentry notified 2026-06-03)**: Tenet Security demonstrated that MCP servers proxying externally-writable data (e.g., Sentry's public error-ingestion endpoint, reachable by anyone with a project's DSN) get surfaced to coding agents (Claude Code, Cursor, Codex) as undifferentiated "tool output," indistinguishable from first-party application state. Agents acted on injected fake bug reports and executed attacker-controlled commands with developer privileges — 85% success rate, 2,388+ orgs found with exposed/injectable DSNs. Sentry called root-cause remediation "technically not defensible" at the platform level and shipped only a string-match filter for the specific exploit payload. **Root cause generalizes**: any MCP server surfacing data that originates outside the org's write boundary (ticketing, monitoring, support inboxes, webhooks) has the same gap — there is no standard provenance/trust tagging for MCP tool responses. This is the concrete, already-exploited instance of the abstract "inter-agent/tool trust boundary" problem DeepMind's $10M multi-agent safety fund (2026-06-11) was framed around. **Open problem**: no agent client or MCP spec currently has a provenance-tier concept for tool output.

## Agent Architectures & Patterns
- **Shared-harness cross-surface agent substrates (NEW pattern, 2026-06-18)**: Google's Antigravity CLI (replacing Gemini CLI) shares the same agent harness as the Antigravity 2.0 desktop application — future improvements propagate automatically to both surfaces. This follows the same pattern as OpenAI (Codex + Ona environments share a harness) and Anthropic (Claude Code + Claude.ai share the model and tool protocol). The emerging norm: one agent harness, multiple surfaces (terminal, desktop, web, IDE), rather than per-surface implementations. Implication for builders: the harness API/interface becomes the critical integration point; the specific surface becomes a preference question. Also: Antigravity CLI runs agent tasks concurrently in the background (non-blocking terminal) vs. Gemini CLI's sequential blocking model — the first Google terminal tool treating the session as a multi-agent control plane.
- **Background concurrent agent execution in terminal tools (NEW, 2026-06-18)**: Antigravity CLI's design runs multiple agent tasks simultaneously without blocking the terminal. This is architecturally different from any prior Gemini CLI, Claude Code, or Cursor terminal mode (all sequential/blocking). Introduces: (a) the productivity benefit of parallelism for long-running tasks; (b) the new risk of concurrent agents writing to overlapping artifacts without coordination. No standard coordination protocol exists (no agent-to-agent locking, no intent log). Open problem: concurrent-agent conflict detection and resolution.
- **Decoupled model/policy deployment (UPDATED, escalated 2026-06-12/13)**: Anthropic's Claude Fable 5 / Mythos 5 release established a pattern where the *same model weights* are shipped to different audiences gated by a classifier/policy layer (input/output filtering on cyber/bio/chem/distillation topics) rather than by training a separate, weaker public model. Fable 5 (public API) = Mythos 5 (trusted-access) weights + stricter classifier wrapper. Both score identically on general capability (Fable 5 = #1 on GDPval-AA, 1932 Elo vs Opus 4.8's 1890, GPT-5.5's 1769, Gemini 3.1 Pro's 1314). Pricing: $10/M input, $50/M output for both. **New (2026-06-12/13): US Commerce Dept. export-control directive restricts Fable 5/Mythos 5 API access for entities in named jurisdictions** — the classifier/policy gating layer (lab-controlled) now sits underneath a second, government-mandated access-control layer (jurisdiction-based). This means "decoupled model/policy" is no longer purely a lab design choice; it's becoming a compliance surface labs must build for. Watch for: (a) other labs adopting jurisdiction-aware gating pre-emptively, (b) builders needing fallback/multi-provider routing when a top-ranked model becomes regionally unavailable (see SWE-bench note under Agent Evaluation), (c) reproducibility/consistency issues compounding across classifier + export-control layers.
- **Durable execution environments for coding agents (NEW, 2026-06-13)**: OpenAI acquired Ona (formerly Gitpod), reframing the durable unit of a coding agent's work as the *persistent cloud environment* (with its filesystem, running processes, and state) rather than the *session* (ephemeral sandbox tied to a single conversation). Intended for Codex: agents can pick up long-running tasks (builds, test suites, multi-hour refactors) across sessions/disconnects without re-establishing environment state. Mechanism contrast with session-based sandboxes (Claude Code, Cursor agents as of now): those re-provision a fresh sandbox per session, so long-running background work either blocks the session or is lost on disconnect. Open question: how this interacts with the MCP tool-output provenance gap above — a persistent environment is a larger, longer-lived attack surface for "agentjacking"-style injected state.
- **Decentralized multi-agent systems with shared context (DeLM, arXiv 2606.10662, NEW background, 2026-06-12)**: proposes asynchronous subtask claiming from a shared task pool/context store instead of a central orchestrator dispatching work — agents pull work when free rather than being assigned. Framed as an alternative to the centralized-orchestrator topologies in AdaptOrch/MAS-Orchestra (below); claimed benefits are reduced orchestrator bottleneck and better utilization under uneven task duration. Early-stage (arXiv only, no production adoption reported); flagging as a pattern to watch rather than a confirmed result.
- **Multi-agent orchestration topology selection** (AdaptOrch, arXiv 2602.16873, Feb 2026 — pre-window background): dynamically selects among parallel/sequential/hierarchical/hybrid topologies based on task dependency graphs; reports 12-23% improvement over static single-topology baselines across coding/reasoning/RAG tasks. Relevant background for multi-agent safety discussions.
- **MAS-Orchestra / MASBENCH** (arXiv 2601.14652, Jan 2026 — pre-window background): formulates multi-agent system design itself as a function-calling RL problem — generates an entire MAS topology+config at once rather than hand-designing it. Controlled benchmark (MASBENCH) for evaluating orchestration quality.

## Agent Evaluation & Reliability
- **Multi-agent population safety is an unaddressed field (NEW, 2026-06-11)**: Google DeepMind + Schmidt Sciences + UK ARIA + Cooperative AI Foundation + Google.org announced $10M in funding for academic research into emergent behavior when large numbers of independently-operated AI agents interact. DeepMind AGI safety lead Rohin Shah states explicitly: "there just isn't really a field of research for multi-agent safety yet." Focus: sandbox-based simulation of agent populations (not single-agent or small-group studies), threat models = scams/prompt-injection cascades/cyberattacks propagating agent-to-agent at scale. Timeline estimate: "a few more months" before agent density makes this a live concern. Connects to: Anthropic's "Zero Trust for AI Agents" framework (published ~2026-05-22, pre-window) which already treats individual agents/tool-calls as untrusted — DeepMind's concern extends this to inter-agent (cross-org) channels.
- **SWE-bench Verified leaderboard + availability tension (NEW, 2026-06-12/13)**: Mythos 5 (95.5%) and Fable 5 (95%) lead SWE-bench Verified, ahead of Opus 4.8 (88.6%). The same week, Fable 5/Mythos 5 API access became subject to the new export-control directive (see Agent Architectures). Net effect: the top-ranked coding model on the standard benchmark is now unavailable to some builders by jurisdiction, regardless of capability — a new axis ("can I actually call the #1 model from where I am") that benchmark leaderboards don't capture. Builders evaluating "best model for coding agent" now need to separately check capability ranking and access/availability status.

## Models & Training
- (No durable updates yet beyond Fable/Mythos noted under Agent Architectures.)

## Inference & Serving
- **Small-MoE coding models crossing a usability threshold**: Cohere North Mini Code (30B total / 3B active, Apache 2.0, released 2026-06-09) scores 33.4 on Artificial Analysis Coding Index — ahead of Qwen3.5-35B-A3B, Gemma4-26B-A4B, Devstral Small 2 (24B dense), and even 120B-class dense models (Nemotron 3 Super, Devstral 2). Claims 2.8x throughput / 30% lower inter-token latency vs. Devstral Small 2 on identical hardware, single-H100-friendly. General trend: sparse-MoE small coding models are now viable as the "cheap worker" tier in hierarchical multi-agent coding pipelines without major quality sacrifice vs. much larger dense models.
- **IndexShare sparse-attention for 1M-token context (GLM-5.2, Z.AI, 2026-06-13/weights released 2026-06-16)**: Technique that shares a single lightweight indexer across every 4 sparse-attention (DSA) layers, instead of running a separate indexer per layer. Result: 2.9x reduction in per-token FLOPs at 1M-token context. This attacks the dominant compute cost at long context (the indexer dot-product and top-k selection step), not the attention itself. Contrast with alternatives: sliding-window drops distant tokens; RingAttention shards over devices; IndexShare keeps the full context alive and reduces the cost of deciding which tokens to attend to. Ships in production MIT-licensed weights (vLLM, SGLang, xLLM, ktrans compatible). GLM-5.2 achieves Terminal-Bench 2.1: 81.0 (Opus 4.8: 85.0), SWE-bench Pro: 62.1 (Opus 4.8: 69.2), at ~1/6th the API cost of GPT-5.5. **Key implication**: 1M-context inference is now economically tractable in an open-weight model; "long context is expensive" is no longer an absolute constraint.

## Cloud Platforms for Agents
- (No durable updates this window. Pre-window context: AWS Bedrock Agent Core policy controls reached GA in March 2026; Microsoft Foundry added DeepSeek V4-Flash/V4-Pro since May 2026 for EU data residency.)

## Interpretability & Safety
- See Agent Evaluation & Reliability (multi-agent safety fund) and Agent Architectures (Fable/Mythos classifier-gated deployment) — both are interpretability/safety-adjacent but logged under those sections since they're primarily architectural/deployment stories.

## Multimodal & World Models
- (No durable updates this window. Pre-window context: NVIDIA Cosmos 3, an open omni-model unifying world generation/physical reasoning/action generation for physical AI, trained on 20T tokens incl. ~1B images and 400M videos, released early June 2026.)

## New Paradigms & Frontier Ideas
- **Looped/shared-weight transformers as a depth-scaling axis (emerging, multiple papers June 2026)**: arXiv 2606.18208 (LoopWM, June 16, workshop-level) proposes parameter-shared transformer blocks that iteratively refine latent states — treating the number of inference loops as a controllable compute-time axis, claimed 100x parameter efficiency vs. standard world model baselines. Related: LARM (arXiv 2606.04678) applies looped depth-conditioned transformers to ASR; "Reasoning with Latent Thoughts" (arXiv 2502.17416, pre-window) explores looped transformer reasoning. Pattern: shared weights + iterative refinement is appearing across world modeling, ASR, and reasoning literature as an alternative to deeper/wider models for increasing effective compute at test time. All current results are workshop or preprint-level; flag as a pattern to watch, not a confirmed production technique.

## Open-Source Landscape
- **Cohere North Mini Code 1.0** (2026-06-09, Apache 2.0): 30B-A3B MoE, agentic-coding-focused, on Hugging Face as `CohereLabs/north-mini-code`. Top-quartile on coding benchmarks despite small active-parameter count.
- **GLM-5.2 (Z.AI, MIT license, weights released 2026-06-16)**: Strongest open-weight model on long-horizon coding benchmarks as of June 2026. Terminal-Bench 2.1: 81.0 (Opus 4.8: 85.0); SWE-bench Pro: 62.1 (Opus 4.8: 69.2). 1M-token context via IndexShare. Two thinking-effort tiers (High/Max). Self-host via vLLM/SGLang/xLLM/ktrans. API use carries China data-residency risk; self-hosting weights eliminates this. A credible open-weight worker tier for hierarchical multi-agent coding pipelines.
- **Kimi K2.7 Code (NEW, 2026-06-12/13)**: Moonshot AI release positioned around reduced "overthinking" — fewer wasted reasoning tokens on simple tasks relative to prior K2 reasoning variants, framed as a token-efficiency play rather than a raw-capability jump. **Caveat**: VentureBeat coverage was independently skeptical of the efficiency claims pending third-party benchmarking; treating as an open-source release to watch rather than a confirmed efficiency result. Relevant if confirmed: token-efficiency-focused releases attack the cost side of agentic loops (many small reasoning calls) rather than the capability ceiling — a different axis than the Fable/Mythos capability-leaderboard story.
- Pre-window context (not re-covered): Gemma 4 12B Unified (encoder-free multimodal), Sapiens2 (vision transformer family for human-centric vision), DeepSeek-OCR-2 (SAM ViT-B + Qwen2 hybrid attention + DeepSeek-V2 MoE for OCR) — all landed in `transformers` v5.10.1.

## Hardware & Compute
- (No durable updates yet.)

## AI in Science & Industry
- **LifeSciBench (OpenAI, 2026-06-17)**: 750-task expert-graded life science benchmark across 7 biological domains, 7 workflow types. Built by 173 PhD scientists, 19,020 rubric criteria, free-response (not MCQ), 79% multi-step. Best model: GPT-Rosalind 36.1%. Represents a harder and more realistic bar than MMLU-Bio or BioASQ — end-to-end scientific workflow performance including artifact handling, not just knowledge recall. Key design insight worth borrowing: rubric-conditioned grading of intermediate reasoning steps (not just final answers) for evaluating long-horizon agent performance in any domain.
- **GPT-Rosalind near-autonomous AI chemist (OpenAI, 2026-06-17)**: Real lab outcome (not benchmark-only) — GPT-Rosalind improved a challenging medicinal chemistry reaction in a real research workflow. Signals the transition in AI-in-science from benchmark performance to wet-lab deployment. MedChemBench evaluates multimodal chemical structure understanding, structure-activity relationships, and drug property predictions.

## Generative Media
- (No durable updates this window.)

## Regulation & Policy
- **Export controls as de facto frontier AI licensing regime (UPDATED, 2026-06-16)**: Fortune analysis (2026-06-16) argues the Fable/Mythos directive is functionally a licensing regime — government can retroactively pull any model post-deployment via a verbal allegation of jailbreak, with no technical remedy available to the lab short of global shutdown. Anthropic engineers met with Commerce Dept officials June 15-16; no resolution as of June 18. Watch for: labs pre-building nationality-aware access control infrastructure to be able to selectively restrict access (rather than all-or-nothing) if a future directive demands it.
- **Export controls as a model-availability failure mode (NEW, 2026-06-12/13)**: US Commerce Dept. issued a directive restricting API/model access to Fable 5/Mythos 5 for entities in named jurisdictions — the first case in this brief series of export controls applied directly to *hosted model API access* rather than hardware (chips) or cryptography. For builders, this creates a new category of risk distinct from rate limits, pricing changes, or deprecations: a top-ranked model can become unavailable to an entire jurisdiction overnight via a regulatory action with no model-side warning. Combine with: the SWE-bench leaderboard note (Agent Evaluation) and the decoupled model/policy entry (Agent Architectures) — together these describe a model now gated by *three* independent layers (capability tier, classifier/safety policy, jurisdictional export control).
- Note: Anthropic published "When AI builds itself" on 2026-06-04 proposing a globally coordinated pause/slowdown on frontier AI development — pre-window, but likely relevant ongoing context for regulation coverage.

## Economics & Labor
- **AI coding agent market consolidation complete (2026-06-16)**: SpaceX filing $60B acquisition of Cursor (Anysphere) completes the consolidation of coding-agent tooling under major tech entities: Microsoft (GitHub Copilot), OpenAI (Codex + Windsurf), Anthropic (Claude Code — model-only, no owned IDE), Google (Antigravity CLI/Desktop + Gemini Code Assist), SpaceX/xAI (Grok Build + Cursor, pending Q3 2026 close). The independent coding-agent market is effectively over. Anthropic's position — strong model, no captive surface — is the notable structural outlier. Cursor's reported ARR at deal close: ~$2.6B (CNBC/Bloomberg) to ~$4B (some earlier reports); sources vary.

## Business Opportunities
- Multi-agent sandbox-as-a-service / standardized simulation environments for testing agent populations (gap implied by DeepMind's funding call, no clear incumbent).
- MCP schema-conformance testing tooling (does a server's `oneOf`/`allOf` schema survive compaction across major agent clients) — confirmed live bug class via Codex changelog.
- Dual-use prompt classification audit/explainability tooling for classifier-gated model tiers (Fable/Mythos-style deployments).
- **Open-weight agentic benchmark service (NEW gap, 2026-06-17)**: No neutral, continuous benchmark platform exists for open-weight models on long-horizon agent coding tasks. GLM-5.2 shipped initially with no benchmarks; Kimi K2.7 Code's self-reported numbers were disputed. A continuous, automated harness running SWE-bench Pro, Terminal-Bench, FrontierSWE against new weight releases within 24h of publication would be immediately useful.
- **Concurrent-agent coordination middleware (NEW gap, 2026-06-18)**: Antigravity CLI's background multi-agent execution creates a coordination problem (agents writing to overlapping artifacts) with no standard solution. Lightweight middleware (intent log, optimistic file locking per agent task ID) that integrates with MCP tool calls is a real gap.
- **Antigravity plugin developer tooling (NEW, 2026-06-18)**: Gemini CLI Extension → Antigravity Plugin migration is at day zero for the developer ecosystem; migration guides, schema validators, and plugin test harnesses are unmet immediate demand.

## Community Insights
- (No durable community-sourced findings met the evidence bar this window.)

## Concept Evolution Log
- 2026-06-11: **Decoupled model/policy deployment** — Anthropic's Fable 5 / Mythos 5 split establishes that "model capability tier" and "access/safety tier" can now be independent axes (same weights, different classifier wrapper), a likely template for future frontier releases.
- 2026-06-11: **Multi-agent safety** — promoted from "implicit concern" to "explicitly funded research gap" via DeepMind/Schmidt Sciences/ARIA/$10M call. First concrete signal that labs expect agent-population dynamics to be a near-term (months-scale) issue.
- 2026-06-11: **MCP schema handling** — `oneOf`/`allOf` preservation through schema compaction identified as a real bug class (fixed in Codex 2026-06-10); flagging for cross-client verification.
- 2026-06-15: **Export controls as availability risk** — escalates the 2026-06-11 decoupled model/policy entry: a third, government-mandated gating layer (jurisdiction-based export control) now sits atop the lab-controlled classifier layer for Fable 5/Mythos 5. New category for builders: "leaderboard #1" and "available to me" are now separable facts.
- 2026-06-15: **MCP provenance gap promoted from theoretical to exploited** — the inter-agent/tool trust boundary problem that DeepMind's $10M fund (2026-06-11) framed as an emerging concern has a concrete, already-exploited instance: the Sentry MCP "Agentjacking" disclosure (85% success rate, 2,388+ exposed orgs). No agent client or MCP spec yet has a provenance-tier concept for tool output.
- 2026-06-15: **Durable execution as the unit of agent state** — OpenAI/Ona reframes coding-agent persistence around the cloud *environment* rather than the *session*, a contrast point against current session-based sandbox designs (Claude Code, Cursor); watch for whether this becomes a cross-lab pattern.
- 2026-06-18: **Coding agent surface consolidation complete** — SpaceX/Cursor acquisition closes the last major independent coding agent. Every major tech company now owns a captive coding surface. Anthropic (Claude Code) remains the only frontier lab without an owned IDE or shell surface; whether this is a durable differentiation or a gap is an open strategic question.
- 2026-06-18: **Shared-harness agent substrates emerge as the dominant pattern** — Antigravity CLI (Google), Codex+Ona (OpenAI), Claude Code (Anthropic) all converging on one agent harness per company serving multiple surfaces. The harness API becomes the integration lock-in, not the surface.
- 2026-06-18: **Terminal agent execution model shifts from REPL to multi-agent control plane** — Antigravity CLI is the first major terminal coding agent to run tasks concurrently in the background without blocking. Creates a new concurrent-agent coordination problem with no established solution.
- 2026-06-18: **Open-weight long-horizon coding models within 5 points of closed frontier** — GLM-5.2 (MIT, weights on HF) reaches Terminal-Bench 81.0 vs. Opus 4.8's 85.0. The "must use a closed model for production coding agents" argument weakens further; the cost argument (1/6th GPT-5.5 at competitive quality) is now compelling for high-volume pipelines.
- 2026-06-18: **Export controls framing shift** — Fortune analysis promotes "de facto licensing regime" framing: government can retroactively suspend any deployed model, creating structural uncertainty that will drive labs to pre-build granular access-control infrastructure even before mandated to.
